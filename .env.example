# Copy this file to .env (same folder as build.gradle) and adjust the values.
# spring-dotenv loads .env automatically on startup.

DB_URL=jdbc:mysql://localhost:3306/QUORA_DB_LOCAL?createDatabaseIfNotExist=true&serverTimezone=UTC
DB_USERNAME=root
DB_PASSWORD=

# Base64 key, >= 32 bytes. Generate with: openssl rand -base64 48
JWT_SECRET=hWWapUSxdq/PNiQ0px5+DjR1grz9tRt3qlgniOya55rVelefe5AYsi9ZCv2EcZ9H

JWT_ACCESS_EXPIRATION_MS=900000
JWT_REFRESH_EXPIRATION_MS=604800000

CORS_ALLOWED_ORIGINS=http://localhost:3000
